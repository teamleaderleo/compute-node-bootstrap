# Ship the VFIO modules in every initramfs so the one-shot Windows entry can
# load them early, but never force-load them. Windows mode asks for the early
# load itself with rd.driver.pre=vfio_pci; a default Linux boot must not.
add_drivers+=" vfio vfio_pci vfio_iommu_type1 "
