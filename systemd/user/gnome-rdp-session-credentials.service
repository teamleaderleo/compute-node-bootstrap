[Unit]
Description=Seed GNOME Remote Desktop credential into the unlocked session keyring
Before=gnome-remote-desktop.service

[Service]
Type=oneshot
ExecStart=%h/.local/bin/seed-big-red-rdp-session-credentials
RemainAfterExit=yes

[Install]
WantedBy=gnome-session.target
